using UnityEngine;
using System.Collections;

public class GameButtonMaster : MonoBehaviour {
	//This script makes sure that if one camera change is occuring you can't do other things

	public static bool ready = true;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
